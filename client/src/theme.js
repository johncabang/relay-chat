import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1982FC",
    },
    secondary: {
      main: "#f44336",
    },
    background: {
      default: "#EDEDED",
    },
  },
});

export default theme;
